import { defineConfig } from 'vite'

// Minimal Vite config. Not using @vitejs/plugin-react to avoid peer dependency
// conflicts in this lightweight example. Vite's default JSX handling via esbuild
// is sufficient for this demo.
export default defineConfig({})
